<% provide(:title, 'All users') %>
<h1>ユーザー一覧</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
<h3>投稿一覧</h3>

  <%= link_to 'サインアップ', new_user_registration_path %>